/** The field's default accent. The renderer resolves grey everywhere and
 *  mixes only the pointer toward this, so a cool near-white leaves the cursor
 *  as the one thing with a temperature. See accent.ts for the full rationale. */
const DEFAULT = '#ddeafe'

export const DEFAULT_ACCENT = DEFAULT
